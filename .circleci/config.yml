version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:current
      - image: docker:dind
        user: root
        entrypoint : ["/usr/local/bin/dockerd-entrypoint.sh --tlsverify=false"]
      
    resource_class: vieer-rubrik/tester
    steps:
      - run: echo "Hi I'm on a Container Runner!"
workflows:
  build-workflow:
    jobs:
      - build